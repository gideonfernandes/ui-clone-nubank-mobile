import React from 'react';

import { Text } from 'react-native';

import { Container } from './styles';

const Tabs = () => {
  return (
    <Container>
      <Text>Tabs</Text>
    </Container>
  );
};

export default Tabs;
